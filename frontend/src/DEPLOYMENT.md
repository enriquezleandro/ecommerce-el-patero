# Deployment

Este documento se movió a la raíz del repo: ver [`/DEPLOYMENT.md`](../../DEPLOYMENT.md).

(La versión anterior de este archivo describía un deploy con Supabase +
Next.js que nunca se usó — la arquitectura real es Vite/React + Express +
Prisma + Neon, documentada en el link de arriba.)
